server {
    listen 80;
    listen [::]:80;
    
    server_name techpro.local localhost;
    
    root /var/www/techpro;
    index index.html;
    
    # Logs personalizados
    access_log /var/log/nginx/techpro/access.log;
    error_log /var/log/nginx/techpro/error.log;
    
    # Página de erro 404 customizada
    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
    
    # Configurações de segurança
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
    
    # Desabilitar listagem de diretórios
    autoindex off;
    
    # Limite de tamanho de upload
    client_max_body_size 10M;
    
    # Configuração principal
    location / {
        try_files $uri $uri/ =404;
    }
    
    # Cache para arquivos estáticos
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # Negar acesso a arquivos ocultos
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }
    
    # Negar acesso a arquivos de backup
    location ~ ~$ {
        deny all;
        access_log off;
        log_not_found off;
    }
}
